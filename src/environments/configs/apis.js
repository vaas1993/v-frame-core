const HOST = 'https://www.demo.com'

export default {
    user: {
        login: {
            url: HOST + "/user/login"
        },
        auth: {
            url: HOST + "/user/auth"
        }
    }
}